<form method="post" action="{{ path('programmer_delete', {'id': programmer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programmer.id) }}">
    <button class="btn">Delete</button>
</form>
